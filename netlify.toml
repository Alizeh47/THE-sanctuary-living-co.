[build]
  publish = "."
  functions = "netlify/functions"

# Removed generic SPA redirects because this is a multi-page static site.
# This prevents form submissions from being redirected to index.html incorrectly.
